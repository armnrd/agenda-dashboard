<Window x:Class="AgendaDashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AgendaDashboard"
        mc:Ignorable="d"
        Title="Agenda Dashboard" Width="416" Height="800"
        ShowInTaskbar="False"
        Topmost="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="500" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <local:GcalView Grid.Row="0" Margin="0,0,0,0" />
        <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                      Background="#EEE" ShowsPreview="True" />
        <local:TodoistView Grid.Row="2" Margin="0,0,0,0" />
    </Grid>
</Window>