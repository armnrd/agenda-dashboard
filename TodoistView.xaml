<UserControl x:Class="AgendaDashboard.TodoistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AgendaDashboard">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ListBox ItemsSource="{Binding TodoistTasks}" Background="Transparent" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="8,4"
                            Padding="12"
                            Background="White"
                            CornerRadius="8"
                            BorderBrush="#DDD"
                            BorderThickness="1">
                            <!-- Effect="{DynamicResource {x:Static SystemParameters.DropShadowKey}}"> -->
                        <DockPanel>
                            <CheckBox IsChecked="{Binding IsCompleted}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Content}"
                                       VerticalAlignment="Center"
                                       FontSize="14"
                                       Foreground="#222"
                                       TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </ScrollViewer>
</UserControl>
